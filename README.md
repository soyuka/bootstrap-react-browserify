# @boot/react-browserify

Create a react-browserify bootstrap

Commands:

```
watch (watchify src/index.js -o index.js -t babelify)
build (browserify src/index.js -o index.js -t babelify)
serve (serve)
```
